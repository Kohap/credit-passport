[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc_version = "0.8.23"
optimizer = true
optimizer_runs = 200
evm_version = "shanghai"
ffi = false
verbosity = 2

[lint]
lint_on_build = false

[rpc_endpoints]
sepolia = "${SEPOLIA_RPC_URL}"
